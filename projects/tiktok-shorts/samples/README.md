# samples — すぐ使えるサンプル

- `topics.json` … `shorts research` が出力するネタ一覧の例（雑学6本）。
- `scripts/*.json` … `shorts script <topic>` が出力する台本の例。
  そのまま `narration` をTTSに渡せば動画にできます。

> ⚠️ 雑学は**正確さが命**です。どのサンプルも投稿前に必ずファクトチェックしてください。
